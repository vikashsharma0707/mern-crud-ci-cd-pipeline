apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: vikashsharma0707/mern-crud-backend:latest   # 👈 Docker Hub image
          ports:
            - containerPort: 5000
          env:
            - name: MONGO_URL
              value: "mongodb+srv://vikashtiwari9893_db_user:GdotgWim8OrpIb3U@cluster0.ne8e1ks.mongodb.net/?appName=Cluster0"
